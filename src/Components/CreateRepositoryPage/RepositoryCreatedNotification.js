export default function RepositoryCreatedNotification({props}){
    const createdRepositoryName = props.repository_name;
    //TODO: indiquer que le repository a été créé
    return(
      <div>

      </div>
    );
}